interface Color {
  red: number;
  blue: number;
  green: number;
  alpha: number;
}

const color: Color = {
  red: 10,
  blue: 10,
  green: 20,
  alpha: 1
}
console.log(color)

export default color;
